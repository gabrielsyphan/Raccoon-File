server:
  error:
    include-stacktrace: on_param

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/file_management?createDatabaseIfNotExist=true
    username: root
    password: fiMang@d4Abs65
  jpa:
    hibernate:
      ddl-auto: update
